{
    "_comments": {
        "_fixed_vs_variable": "Fixed params (scalars) are shared by all runs. Variable params (arrays) create a grid of combinations.",
        "master_seed": "Seed for reproducibility",
        "num_seeds": "0 = use master_seed directly; N > 0 = derive N seeds from master_seed"
    },
    "exp_name": "experiment",
    "master_seed": 42,
    "num_seeds": 0,
    "iterations": 10,
    "output_dir": "outputs",
    "N": [10, 20],
    "prob": [0.1, 0.5],
    "slurm": {
        "_comments": {
            "cores": "CPUs per task (--cpus-per-task)",
            "nodes": "Number of nodes (--nodes)",
            "time": "Max runtime in D-HH:MM format (--time)",
            "memory": "Memory per job (--mem)",
            "partition": "SLURM partition (--partition), leave empty for default",
            "account": "Account/project for billing (--account), leave empty if not required",
            "email": "Email for notifications (--mail-user), leave empty to disable",
            "email_type": "When to email: NONE, BEGIN, END, FAIL, ALL (--mail-type)",
            "log_dir": "Directory for stdout/stderr logs",
            "job_name": "Job name shown in squeue (--job-name)",
            "exclude": "Nodes to exclude (--exclude), leave empty for none",
            "constraint": "Node constraints (--constraint), leave empty for none",
            "gres": "Generic resources like GPUs (--gres), e.g. 'gpu:1', leave empty for none"
        },
        "cores": 1,
        "nodes": 1,
        "time": "0-02:59",
        "memory": "5G",
        "partition": "",
        "account": "",
        "email": "",
        "email_type": "FAIL",
        "log_dir": "outputs/slurm_logs",
        "job_name": "experiment",
        "exclude": "",
        "constraint": "",
        "gres": ""
    }
}